export function TracksPlaceholder() {
  return (
    <div style={{
      textAlign: 'center',
      fontFamily: 'var(--font-body)',
      fontSize: 15,
      color: 'var(--gold-dark)',
    }}>
      No tracks yet — check back Friday
    </div>
  );
}
