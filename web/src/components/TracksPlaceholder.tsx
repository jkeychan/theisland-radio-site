export function TracksPlaceholder() {
  return (
    <div style={{
      textAlign: 'center',
      fontFamily: 'var(--font-ui)',
      fontSize: 13,
      color: 'var(--gold-mid)',
    }}>
      No tracks yet — check back Friday
    </div>
  );
}
